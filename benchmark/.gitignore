.venv/
target/
# Public instance data and source files are part of the reproduction package.
__pycache__/
.pytest_cache/
node_modules/
work/
workbook_build/
*.pyc
*.tmp
*.log
# Only the declared completed experiment belongs in this publication tree.
/results/*
!/results/scaling-20260924-current-parallel/
# Cargo dependency locks are reproducibility metadata, including frozen copies.
!**/Cargo.lock
# The measured algorithm is distributed as one source archive.
!/algorithm-9bc9dbf.zip
