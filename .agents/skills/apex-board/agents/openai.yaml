interface:
  display_name: "APEX Board"
  short_description: "Maintain the APEX board with approved changes"
  default_prompt: "Use $apex-board to organize these ideas and propose their classification before changing the board."
policy:
  allow_implicit_invocation: false
